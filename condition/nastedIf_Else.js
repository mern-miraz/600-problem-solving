const friendsInvite = true;
if (friendsInvite) {
  console.log("I want to join friends birthday party");
  const pocketMoney = 1000;
  if (pocketMoney > 1000) {
    console.log("This gift to you");
  } else {
    console.log("I am going without money");
  }
} else {
  console.log("Remove friend from the list.");
}

const guest = true;
if (guest) {
  const tea = true;
  if (tea) {
    const biscuit = true;
    if (biscuit) {
      console.log("biskut khaben?");
    } else {
      console.log("your tea is ready");
    }
  } else {
    console.log("watching the star jalsa");
  }
}

const isActive = true;
if (isActive) {
  const isSubscription = true;
  if (isSubscription) {
    const isPremium = false;
    if (isPremium) {
      console.log("you are watching premium version");
    } else {
      console.log("watch the free version");
    }
  }
}

const isFood = true;
if (isFood) {
  console.log("Heat up the food and eat.");
} else {
  const isDelivery = true;
  if (isDelivery) {
    console.log("order the food by app");
  } else {
    console.log("ajke ami roja");
  }
}

const guestList = 110;
if (guestList > 100) {
  const isGift = true;
  if (isGift) {
    console.log("Lets party all night");
  } else {
    console.log("I will party with my self");
  }
}
